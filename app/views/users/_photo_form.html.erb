<%= form_for current_user do |f| %>
  <div><%= hidden_field_tag :update_photo, 1 %></div>
  <div class="picture">
    <%= f.file_field :photo, accept: 'image/jpeg,image/gif,image/png' %>
  </div>
    <%= f.submit 'Change', class: 'btn btn-primary' %>
<% end %>
