<% provide(:title, 'Edit user') %>
<% provide(:botton_text, 'Save changes') %>
<% provide(:form_action, user_path) %>

<h1>Update your profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
    <!--
    <div class="gravater_edit">
      <%= photo_for @user %>
    </div>
    -->
  </div>
</div>
